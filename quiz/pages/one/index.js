import styled from "@emotion/styled";

export default function LayoutSection() {
  const Wrapper = styled.div`
    height: 70px;
    background: green;
  `;


  return <Wrapper>one 영역입니다.</Wrapper>;
}
